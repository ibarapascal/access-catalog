/*
 * @lc app=leetcode id=166 lang=javascript
 *
 * [166] Fraction to Recurring Decimal
 */
/**
 * @param {number} numerator
 * @param {number} denominator
 * @return {string}
 */
var fractionToDecimal = function(numerator, denominator) {



    // var cal = parseFloat(numerator / denominator);
    // var calStr = cal.toString();
    // if (!calStr.some(i => i === '.')) {
    //     return calStr;
    // } else {
    //     var checkLength = 10;
    //     var pointIndex = calStr.indexOf('.')
    //     var checkStr = calStr.slice(pointIndex + 1, calStr.length);
    //     for (let i = 1; i <= checkLength; i++) {
            
    //     }
    // }


    // 2019/08/16
    // step 0: check if point
    // step 1: get 100 str
    // step 2: check if circle
        // from bottom
        // diff index
        // repeat
        // find out the step of circle
    // step 3: find the circle

};

